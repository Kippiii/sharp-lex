/// ===========================================
/// Module for dealing with modified NFAs
/// ===========================================
module ModNFA =

    // Type for a modified NFA
    type ModNFA = todo

    // Generate a modified NFA from regex
    let fromRegex re = todo

    // Merge two modified NFAs
    let merge nfa1 nfa2 = todo

    // Create a DFA from a modified NFA
    let toDFA nfa = todo