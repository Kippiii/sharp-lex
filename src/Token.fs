/// ===========================================
/// Module for tokens
/// ===========================================
module Token =

    // Type for tokens
    type Token = todo

    // Create a token
    let makeToken id content = todo

    // Get the id of a token
    let getId t = todo

    // Get content of a token
    let getContent t = todo