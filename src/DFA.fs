/// ===========================================
/// Module for DFAs
/// ===========================================
module DFA =

    // The type for a DFA
    type DFA = todo

    // Create a DFA
    let makeDFA states start final error transition = todo

    // Run a DFA
    let run dfa input = todo

    // Convert a DFA to a file
    let toFile dfa = todo

    // Generate a DFA from a file
    let fromFile input = todo