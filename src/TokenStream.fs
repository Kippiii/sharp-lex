/// ===========================================
/// Module for streams of tokens
/// ===========================================
module TokenStream =

    // Type for a token stream
    type TokenStream = todo

    // Make a token stream from a list
    let makeStream ts = todo

    // Add to a token stream
    let add ts t = todo

    // Convert a stream to a file
    let toFile ts = todo